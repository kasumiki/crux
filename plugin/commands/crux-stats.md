---
name: stats
description: "Show Crux compression statistics and token savings"
---

Run the Crux stats command to display savings:

```bash
crux stats
```

If the `crux` CLI is not in PATH, use:

```bash
python3 "${CLAUDE_PLUGIN_ROOT}/crux/cli.py" stats
```

Present a summary of tokens saved in the current session and overall.
